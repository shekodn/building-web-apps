1. Run
  npm install

2. Run
  node index.js
to launch two web servers, localhost:8081 and localhost:8083.

3. Access http://localhost:8081 and click the buttons on the page to
send Ajax requests, and watch the messages in the browser's JS console.

TODO (For assignment #5):
In order to be able to send CORS Ajax request with cookies
to http://localhost:8083, you will need to modify "index.html" 
and "index2.js" accordingly.

